<%= form_with(model: estudio) do |form| %>
  <% if estudio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(estudio.errors.count, "error") %> prohibited this estudio from being saved:</h2>

      <ul>
        <% estudio.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.datetime_select :date %>
  </div>

  <div class="field">
    <%= form.label :paciente_id %>
    <%= form.number_field :paciente_id %>
  </div>

  <div class="field">
    <%= form.label :estudio_type_id %>
    <%= form.number_field :estudio_type_id %>
  </div>

  <div class="field">
    <%= form.label :comments %>
    <%= form.text_area :comments %>
  </div>

  <div class ="control-group">
    <%=form.label :paciente, class: "control-group"%>
    <div class ="controls">
      <%= collection_select(:paciente, :paciente_id, Paciente.all, :id, :name) %>




  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
