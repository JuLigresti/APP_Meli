<p id="notice"><%= notice %></p>

<div class="jumbotron">
  <h1 class="display-4"><%=@paciente.first_name + " " +  @paciente.last_name%></h1>
  <p class="lead"><%= @paciente.historial %></p>
  <hr class="my-4">
  <p><%= "Tel. " + @paciente.phone + " | " + "Email: " + @paciente.email%></p>
</div>

<%= link_to 'Edit', edit_paciente_path(@paciente), class: "btn btn-secondary" %>
<%= link_to 'Atrás', pacientes_path, class: "btn btn-secondary" %>